"use strict";

class GB_MAPPER_MBC1 {
    /**
     * @param {GB_bus} bus
     * @param {GB_clock} clock
     */
    .constructor(clock, bus) {
        this.bus = bus;
        this.clock = clock;
    }
}